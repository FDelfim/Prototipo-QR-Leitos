import React, { useState } from "react";
import { View, Text, TextInput, TouchableOpacity } from "react-native";
import database from '../../../config/firebase.js'

export default function Leito({ navigation }) {


    return (
        <View>
            <Text>
                Leitos
            </Text>
        </View>
    );

}